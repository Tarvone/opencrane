# Cognee

This application owns the release-local Cognee workload and its Service, storage, and network-policy resources. Its Helm library exports named templates; `apps/opencrane-infra` only composes them with the parent release context.
