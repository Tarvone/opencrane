# LiteLLM

This application owns the release-local LiteLLM Deployment, Service, and generated Secret. Shared mode renders none of those managed resources and instead uses the configured shared endpoint and credentials.
