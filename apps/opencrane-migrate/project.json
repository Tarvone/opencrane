{
  "name": "opencrane-migrate",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": ["type:app", "layer:entrypoint", "scope:opencrane-migrate"],
  "targets": {
    "helm-lint": {
      "executor": "nx:run-commands",
      "options": { "command": "helm lint apps/opencrane-migrate/helm" }
    }
  }
}
