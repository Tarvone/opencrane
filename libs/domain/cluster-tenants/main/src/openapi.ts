/**
 * OpenAPI paths for the cluster-tenants domain.
 * (No paths in the clustertenant-operator spec)
 */
/** OpenAPI path fragments owned by the cluster-tenants domain (composed into the control-plane spec). */
export const _ClusterTenantsOpenapiPaths = {};
