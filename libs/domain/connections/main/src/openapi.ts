/**
 * OpenAPI paths for the connections domain.
 * (No paths in the clustertenant-operator spec)
 */
/** OpenAPI path fragments owned by the connections domain (composed into the control-plane spec). */
export const _ConnectionsOpenapiPaths = {};
