/** Representative settings draft exercised by the shared reference form. */
export interface SettingsProfileDraftFixture
{
	/** Public name entered by the user. */
	readonly displayName: string;
	/** Whether workspace notifications are enabled. */
	readonly notificationsEnabled: boolean;
}
