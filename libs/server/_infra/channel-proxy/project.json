{
  "name": "server-infra-channel-proxy",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/server/_infra/channel-proxy/src",
  "tags": ["type:lib", "layer:infra", "scope:channel-proxy"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "tsc -p tsconfig.json --noEmit", "cwd": "libs/server/_infra/channel-proxy" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run --passWithNoTests", "cwd": "libs/server/_infra/channel-proxy" } }
  }
}
