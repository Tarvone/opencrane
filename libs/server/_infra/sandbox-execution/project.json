{
  "name": "server-infra-sandbox-execution",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/server/_infra/sandbox-execution/src",
  "tags": ["type:lib", "layer:infra", "scope:sandbox-execution"],
  "targets": {
    "lint": { "executor": "nx:run-commands", "options": { "command": "tsc -p tsconfig.json --noEmit", "cwd": "libs/server/_infra/sandbox-execution" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "vitest run", "cwd": "libs/server/_infra/sandbox-execution" } }
  }
}
