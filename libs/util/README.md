# @opencrane/util — shared pure helpers

Dependency-free helpers shared across domain packages, including collection operations and
RFC 8785 JSON canonicalization with explicitly encoded SHA-256 digests. Tagged `scope:shared`:
it may never import from a domain package or an app.
